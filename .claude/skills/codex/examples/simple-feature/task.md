# Task: Fibonacci Function

Create a TypeScript function that calculates Fibonacci numbers.

## Requirements

- Function named `fibonacci` in `src/math.ts`
- Accepts a non-negative integer `n`
- Returns the nth Fibonacci number (0-indexed)
- Uses efficient algorithm (not naive recursion)
